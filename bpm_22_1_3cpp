#include <iostream>
#include <string>
int main()
{
    std::string slovo;
    std::cin >> slovo;
    int result(0);
    for (int i(0); i < slovo.length(); i++) {
        char bykva1, bykva2;
        std::cin >> bykva1 >> bykva2;
        if ((bykva1 != 'a') && (bykva1 != 'o') && (bykva1 != 'u') && (bykva1 != 'i') && (bykva1 != 'e') && (bykva1 != 'n')) {
            if ((bykva2 != 'a') && (bykva2 != 'o') && (bykva2 != 'u') && (bykva2 != 'i') && (bykva2 != 'e') && (bykva2 != 'n')) {
                
        }
    }
}
