#include <iostream>
#include <vector>
int main() {
    int t(0);
    std::cin >> t;
    for (int i(0); i < t; i++) {
        int tek(0);
        char start;
        char s;
        std ::cin >> tek;
        std::cin >> start;
        std::cin >> s;
        s2 = s + s;
        int max(0);
        
        
    }
}